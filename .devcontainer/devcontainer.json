{
    // Use Docker Compose instead of a single image
    "dockerComposeFile": "docker-compose-copy.yml",
    "service": "devcontainer",
    "workspaceFolder": "/workspace"

    // Add other necessary configurations here
}